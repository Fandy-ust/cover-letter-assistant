# Raw Inputs

This folder contains all user-provided source inputs for the pipeline.

## Subfolders
- `raw_inputs/profile/`: CV, resume, LinkedIn export, and supporting profile notes.
- `raw_inputs/job/`: raw job posts and any role-specific context notes.
- `raw_inputs/style_samples/`: past cover letters and writing-style reference notes.

## Recommended format
Use `.md` when possible for notes you create manually. `.pdf` and `.txt` are also supported.
